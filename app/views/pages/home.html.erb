<div class="container py-5">
  <div class="row">
    <div class="col-md-8 mx-auto">
      <% if user_signed_in? %>
        <% @profile = current_user.profile %>
        <% if current_user.manager? %>
          <%= link_to 'Add freelancers', '#', class: "btn btn-primary" %>
        <% end %>
      <% end %>
    </div>
  </div>
</div>
