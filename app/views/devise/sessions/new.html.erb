<div class="background-blue">
  <div class="row">
    <div class="col-md-6 col-sm-12 d-flex justify-content-center">
      <div class="w-75 my-5 d-flex justify-content-center align-items-center">
        <%= cl_image_tag "Illustration_wky126", :height=>600, :quality=>100 %>
      </div>
    </div>
    <div class="col-md-6 col-sm-12 d-flex justify-content-center align-items-center">
      <div class="login w-75">
        <h2 class="mt-5">Sign in</h2>
        <div class="align-items-center p-5">
            <%= simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
              <div class="form-inputs">
                <%= f.input :email,
                            required: false,
                            autofocus: true,
                            input_html: { autocomplete: "email" } %>
                <%= f.input :password,
                            required: false,
                            input_html: { autocomplete: "current-password" } %>
                <%= f.input :remember_me, as: :boolean if devise_mapping.rememberable? %>
                <div class="form-actions">
                  <%= f.button :submit, "Log in", class: "btn btn-pri mb-4" %>
                </div>
              </div>
              <%= render "devise/shared/links" %>
            <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
